/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: this file is normally auto-generated the first time you run
// `next dev` or `next build`. It's checked in here as a placeholder so the
// project has the file `tsconfig.json` expects; Next.js will overwrite it
// on first run, which is expected and fine.
