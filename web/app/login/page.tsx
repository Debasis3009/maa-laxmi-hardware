'use client';
import {useState,useTransition} from 'react';import {useRouter} from 'next/navigation';import {loginWithCredentials,verifyLoginOtp} from '@/app/actions/auth';
export default function LoginPage(){const[error,setError]=useState<string|null>(null),[otp,setOtp]=useState(false),[devCode,setDevCode]=useState('');const[pending,startTransition]=useTransition();const router=useRouter();
 function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError(null);const fd=new FormData(e.currentTarget);startTransition(async()=>{const r=await loginWithCredentials(fd);if(r.error)setError(r.error);else if(r.otpRequired){setOtp(true);setDevCode(r.devCode||'');}})}
 function verify(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError(null);const fd=new FormData(e.currentTarget);startTransition(async()=>{const r=await verifyLoginOtp(String(fd.get('code')||''));if(r.error)setError(r.error);else{router.push('/admin');router.refresh();}})}
 return <div className="mx-auto max-w-sm px-4 py-16"><div className="rounded-2xl border bg-white p-6 shadow-sm"><p className="text-[10px] font-bold uppercase tracking-[.2em] text-[#07527f]">MAA LAXMI HARDWARE</p><h1 className="mt-1 text-2xl font-black text-slate-900">Staff Login</h1><p className="mt-1 text-xs text-slate-500">Secure Admin / Worker access with WhatsApp verification.</p>{error&&<div className="mt-4 rounded-xl border border-rose-200 bg-rose-50 p-3 text-xs font-semibold text-rose-700">{error}</div>}{!otp?<form onSubmit={submit} className="mt-5 space-y-4"><label className="block text-xs font-bold uppercase text-slate-600">Role<select name="role" className="mt-1.5 w-full rounded-xl border p-3 text-sm font-medium text-slate-900"><option value="ADMIN">Admin</option><option value="WORKER">Worker</option></select></label><label className="block text-xs font-bold uppercase text-slate-600">User ID<input name="username" required autoComplete="username" placeholder="Enter User ID" className="mt-1.5 w-full rounded-xl border p-3 text-sm font-normal normal-case text-slate-900"/></label><label className="block text-xs font-bold uppercase text-slate-600">Password<input type="password" name="password" required autoComplete="current-password" placeholder="Enter Password" className="mt-1.5 w-full rounded-xl border p-3 text-sm font-normal normal-case text-slate-900"/></label><button disabled={pending} className="w-full rounded-xl bg-[#07527f] py-3 text-sm font-bold text-white disabled:opacity-50">{pending?'Checking...':'Continue to WhatsApp verification'}</button></form>:<form onSubmit={verify} className="mt-5 space-y-4"><div className="rounded-xl bg-sky-50 p-3 text-xs text-sky-900">A 6-digit verification code is required from the registered WhatsApp number ending in <b>7908</b>.</div>{devCode&&<div className="rounded-xl bg-amber-50 p-3 text-xs text-amber-900">Development code: <b>{devCode}</b></div>}<label className="block text-xs font-bold uppercase text-slate-600">Verification code<input name="code" inputMode="numeric" pattern="[0-9]{6}" maxLength={6} required autoComplete="one-time-code" placeholder="6-digit code" className="mt-1.5 w-full rounded-xl border p-3 text-center text-xl font-bold tracking-[.35em] text-slate-900"/></label><button disabled={pending} className="w-full rounded-xl bg-[#07527f] py-3 text-sm font-bold text-white disabled:opacity-50">{pending?'Verifying...':'Verify & Sign In'}</button><button type="button" onClick={()=>{setOtp(false);setError(null)}} className="w-full text-xs font-bold text-slate-500">← Back to login</button></form>}</div></div>}
